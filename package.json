{
  "name": "co-vectorize",
  "version": "1.0.0",
  "description": "globally consistent event ordering for decentralized distributed systems",
  "scripts": {
    "build": "tsc"
  },
  "devDependencies": {
    "prettier": "^2.4.1",
    "typescript": "^4.4.3"
  }
}
